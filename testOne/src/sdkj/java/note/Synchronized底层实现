synchronized底层实现
JDK早期  重量级   - OS找操作系统去申请锁  很慢
后来改进
锁升级的概念
sync(Object)
    第一个线程访问时 markwork 记录这个线程ID（偏向锁 其实没加锁）
如果有线程征用 升级为自旋锁(cas) (不访问操作系统)
旋十次以后若还得不到这把锁 升级为重量级锁

延伸
执行时间长的任务适合使用系统锁
短的适合用自旋锁

锁只能升级不能降级